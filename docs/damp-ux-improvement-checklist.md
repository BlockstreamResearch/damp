# DAMP UX improvement checklist

This is the source of truth for the current review pass. Status is evidence-based: **implemented**, **pending**, **blocked**, or **deferred**.

| # | Requirement and acceptance criteria | Status | Evidence / protocol constraint |
|---|---|---|---|
| 1 | Preserve approved blacklist feedback: spendable/blocked balances, output state, draft review/undo, receipt, mobile and accessibility. | implemented | Existing regulated-funds, blacklist-table and receipt implementation/tests remain; all 188 web tests pass. |
| 2 | Replace ReceiveRecord JSON/URL exchange with one confidential address; reject malformed, unconfidential, wrong-network and incompatible covenant addresses; remove record UI/types/storage/parser/schema/docs. | implemented | Rust `receive/transfer/reissuance` and web wallet/validation/storage changes plus negative tests. Holder pubkey is encoded as the address blinding key. This proves selected covenant compatibility, not unique manifest provenance if manifests deliberately share every covenant parameter; the Send UI discloses that boundary. BIP322 alias/proof exchange is removed. Existing regulated outputs are explicit and the covenant script is unchanged, so IndexedDB v5 safely deletes old records. |
| 3 | Make all action labels reflect actual effects, phases, fees, network, faucet privacy and mnemonic persistence. | implemented | Send and reissue say `Sign and broadcast`; policy activation exposes validation/broadcast/confirmation phases with `aria-busy`; exact fees, network-aware messages, external-faucet address disclosure, synchronization wording, true draft discard, and disconnect persistence copy are present. |
| 4 | Semantic responsive UTXO/list layouts with labeled Outpoint/State/Asset/Amount, units, complete mobile identity and accessible states. | implemented | Wallet UTXOs use a semantic table and labeled mobile cards without hiding asset identity; compact output rows are labeled; registry actions, receipt values, and issuer Holder output rows have responsive overflow-safe layouts. |
| 5 | Holder onboarding routes: official GitHub sync, custom GitHub URL, Advanced manual import, Create asset CTA; Issuer Setup has Create/Import; remove empty card whitespace. | implemented | Holder import exposes official-registry status, a default-branch-pinned custom GitHub manifest route, Advanced manual import, and Create CTA. Custom deployments persist their source repository; policy cache keys include that source and every resolution re-verifies its current canonical bytes before use. Issuer Setup has Create/Import plus direct control attachment and defaults to the connected signer's network; choice cards no longer impose empty height. |
| 6 | Requested issuance must not appear as unexplained 499+1; emit one 500 recipient output if safe, otherwise explicitly represent the required output; exact output tests. | implemented | The split was not a covenant/state requirement: both outputs had the same regulated asset and holder script. Bootstrap and reissuance now each emit one exact regulated output; lifecycle assertions verify its asset, value and uniqueness while the verifier anchor, token, fee and change outputs remain distinct. |
| 7 | Remove questionable logo/trademark artwork; retain functional icons and accessible neutral branding. | implemented | Repository audit found no raster/SVG/favicon/logo asset to retain. The prior decorative brand mark was removed; user-facing brand is neutral text, while functional Lucide icons remain. |
| 8 | Put Holder/Issuer mode and deployment selector prominently at top with keyboard, responsive and state handling; remove old duplication. | implemented | A single responsive workspace bar precedes page controls and exposes current mode/switch plus deployment loading/error/empty/select states; old rail-bottom duplicates are gone. |
| 9 | Issuer tabs/default: Setup, Blacklist, Reissue, Holder, Report; preserve deep links and keyboard behavior. | implemented | Issuer navigation and router use the exact requested order, `/admin` redirects to Setup, and all tabs have dedicated routes. |
| 10 | Issuer Holder tab lists all known selected regulated-asset UTXOs without inventing identity, with semantic responsive states. | implemented | `AdminHolders` scopes the signer-observable wallet inventory to the selected regulated asset, labels unavailable identity honestly, and distinguishes loading/empty/error/status states in a semantic responsive table. |
| 11 | Report tab contains only `TODO: In the future, this tab can support reporting to the regulator.` | implemented | Dedicated route renders the exact placeholder and no controls. |
| 12 | Per-UTXO Block/Undo changes deployment-scoped draft only, accurately handles all states, links to Blacklist review, and has no publication side effect. | implemented | Holder actions call only the deployment/policy-scoped draft store; updates are serialized against the latest in-memory draft to prevent rapid-click lost updates; active/pending/ineligible/drafted states are gated; focused helper tests cover successive additions, add/undo, cross-deployment rejection, pending rejection and active-blacklist rejection. |
| 13 | Remove internal/security self-description that does not aid decisions; retain actionable rules, fees, privacy, risks and irreversible warnings. | implemented | Internal `Fail closed` prose was removed; remaining copy explains user-visible constraints, source trust, explicit regulated asset/value visibility, confidential addresses, exact fees and irreversible broadcasts. Raw issuance configuration moved under Advanced disclosure. |
| 14 | Reuse/split a single funding UTXO safely, avoiding unnecessary/dust/covenant-invalid splits and previewing explicit split fees/results. Clearly emphasize the split action whenever one useful confirmed output is the only funding source. | implemented | `split.rs` builds a signer-owned one-input/two-output wallet transaction with a bounded exact 500-sat fee, 1,001-sat output floors, value/proof verification, fixed watched destinations and a split→bootstrap distinct-asset lifecycle test. `issuanceFundingPlan` offers it only for one confirmed output ≥5,000 sats with no pending alternative. `FundingSplitDialog` persists the signed hex, previews exact conservation, serializes signing, retries only that hex, restores after reload, tracks confirmation, and exposes keyboard/dialog states. |
| 15 | User-facing branding/SEO is `DAMP — Decentralized Asset Management Protocol`; retain compatibility-sensitive internal AMP IDs/packages/storage/routes and document exceptions. | implemented | Shell, page title, README, schema titles, public copy and OpenGraph/Twitter metadata use DAMP and the expansion. Internal `simplicity-amp/v0.1`, registry schema values, package/crate names, IndexedDB name, DOM IDs and source URLs remain for compatibility. |
| 16 | Persistent accessible responsive warning: experimental proof of concept, not production-ready, no real funds/assets, use at own risk; not permanently dismissible. | implemented | Non-dismissible top `role=note` warning appears before workspace controls in every app-shell route; README repeats it. |
| 17 | Holistic newcomer reduction: fewer clicks, progressive Advanced controls, highlighted primary actions, useful context only, explicit irreversible confirmation; incorporate Fable P0/P1. | implemented | Fable high audit P0/P1 incorporated: Setup default, discoverable signer callout, consolidated top controls, address-only send, Holder block-to-draft, source-choice onboarding, direct issuer attachment, Advanced raw/manual controls, broadcast labels and confirmed destructive draft discard. Protocol-required registry merge/review gates remain. |
| 18 | Full Rust/protocol/web tests, typecheck/build/schema/format/clippy/diff and desktop/mobile/accessibility QA; empty index, no commit/push. | implemented | All 198 web tests, typecheck, production build, schema, Rust protocol/core/signer, strict Clippy and formatting checks pass. All three Elements regtests pass through `scripts/test-regtest.sh`; Daybreak rendered Browser QA at 1280/768/375/320 for the prior closed scope. The new split dialog has focused keyboard, retry/idempotency, stale-funding, restore and preview tests; a live funded split broadcast still requires the external Elements/Liquid harness. The production build has only the existing large-chunk warning. |
| 19 | Every generated registry download shows the exact canonical GitHub destination path in full, visually emphasized and responsive, with an accessible one-click Copy path action and success/failure feedback. | implemented | Shared `RegistryPathRow` is used by both bootstrap files and successor-policy publication. It renders the exact path variable with `overflow-wrap:anywhere` and no ellipsis, while `ClipboardCopyButton` copies the byte-identical repo-relative value. Focused tests cover full rendering, exact clipboard bytes, success and failure states. |

## Audit matrix

| Area | Files | Automated evidence | Manual QA |
|---|---|---|---|
| Address protocol | `crates/amp-signer/src/{receive,transfer,reissuance,bootstrap,model,lib}.rs`, `crates/amp-core/src/registry.rs` | lifecycle/protocol and address negative tests | Receive QR/copy and Send paste/review on both networks |
| Web/storage migration | `apps/web/src/lib/{amp-signer,transfer-validation,wallet-sync,store}.ts`, wallet/admin screens | validation, wallet-send, store-upgrade, sync tests | upgraded IndexedDB reload and old explicit UTXO rescan |
| Blacklist | regulated funds/table/receipt and wallet/admin | focused balance/draft/receipt tests | partial/full block, undo, receipt, mobile |
| Shell/onboarding | `components/ui.tsx`, wallet/admin, `github.ts`, `deployments.ts`, `styles.css` | title/role navigation, custom-source default-branch and catalog reconciliation tests | 320/375/768/1024/wide, keyboard/focus |
| Issuer Holder/Block/Report | admin, `holder-blacklist-draft.ts`, blacklist drafts, wallet sync | add/undo, cross-deployment, pending and active-blacklist tests | every UTXO state and mobile actions |
| Branding/risk/SEO | app shell, HTML/manifest/assets, README/docs | metadata boundary checks to add | screen-reader and visual hierarchy |
| Funding/issuance | bootstrap/reissuance/split/transaction/planner/dialog | exact regulated-output tests; split conservation, confidential inputs, structural rejection, split→bootstrap lifecycle, planner boundary, retry/idempotency and focus tests | funded one-UTXO split on testnet/regtest, confirmation and reload |
| Registry publication paths | `registry-path-row`, admin publish/bootstrap file rows, styles | exact DOM/clipboard path and clipboard-failure tests | wrap and copy at 320/375/768/1280 px |

Items may be marked implemented only with file-level evidence, automated checks where feasible, and the listed manual QA.
